name: webserv CI
run-name: Run make on ${{ github.ref_name	 }}

on:
  push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: make
        run: make
